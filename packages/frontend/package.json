{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "esbuild": "esbuild main.js --bundle --outfile=dist/bundle.js"
  },
  "devDependencies": {
    "vite": "^3.1.0"
  },
  "dependencies": {
    "esbuild": "^0.15.10"
  }
}
